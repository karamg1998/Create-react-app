import './interested.css'
import Navbar from '../navbar/Navbar';

function Interested()
{
    return(
        <div className="similar-jobs">
        <Navbar></Navbar>
        <h1>this is interested container</h1>
        </div>
    );
}

export default Interested;